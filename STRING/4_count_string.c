/*4. Write a program in C to count the total number of 
words in a string.*/

#include<stdio.h>
main()
{
	char str[20];
	int i,count=0;
	
	printf("Enter the String=");
	scanf("%s",&str);
	
	for(i=0;a[i]!=0;i++)
	{
		count++;
	}
	printf("\n\n\t=%d",count);
}
